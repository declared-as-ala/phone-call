VITE_ENABLE_MOBILE_SIMULATOR=false

# Optional: only when not using `npm run dev` (Vite normally proxies /api and /ws to :8000).
# VITE_API_BASE_URL=http://localhost:8000
# VITE_WS_URL=ws://localhost:8000/ws
