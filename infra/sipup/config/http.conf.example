; Copy to http.conf — HTTP server for ARI (and optional static).
; Replace bindaddr if you only want localhost on the host (not inside Docker).

[general]
enabled = yes
bindaddr = 0.0.0.0
bindport = 8088
prefix = asterisk
enable_static = yes
